//===----------------------------------------------------------------------===//
//
// This source file is part of the Teco open source project.
//
// Copyright (c) 2022 the Teco project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by TecoServiceGenerator.
// DO NOT EDIT.

extension TCDcError {
    public struct UnsupportedOperation: TCErrorType {
        enum Code: String {
            case crossBorderDirectConnectTunnel = "UnsupportedOperation.CrossBorderDirectConnectTunnel"
            case stateConfLict = "UnsupportedOperation.StateConfLict"
            case other = "UnsupportedOperation"
        }
        
        private let error: Code
        
        public let context: TCErrorContext?
        
        public var errorCode: String {
            self.error.rawValue
        }
        
        /// Initializer used by ``TCClient`` to match an error of this type.
        ///
        /// You should not use this initializer directly as there are no public initializers for ``TCErrorContext``.
        public init ?(errorCode: String, context: TCErrorContext) {
            guard let error = Code(rawValue: errorCode) else {
                return nil
            }
            self.error = error
            self.context = context
        }
        
        internal init (_ error: Code, context: TCErrorContext? = nil) {
            self.error = error
            self.context = context
        }
        
        /// 不允许创建跨境专用通道，请您联系我们。
        public static var crossBorderDirectConnectTunnel: UnsupportedOperation {
            UnsupportedOperation(.crossBorderDirectConnectTunnel)
        }
        
        /// 状态冲突。
        public static var stateConfLict: UnsupportedOperation {
            UnsupportedOperation(.stateConfLict)
        }
        
        /// 操作不支持。
        public static var other: UnsupportedOperation {
            UnsupportedOperation(.other)
        }
    }
}

extension TCDcError.UnsupportedOperation: Equatable {
    public static func == (lhs: TCDcError.UnsupportedOperation, rhs: TCDcError.UnsupportedOperation) -> Bool {
        lhs.error == rhs.error
    }
}

extension TCDcError.UnsupportedOperation: CustomStringConvertible {
    public var description: String {
        return "\(self.error.rawValue): \(message ?? "")"
    }
}

extension TCDcError.UnsupportedOperation {
    /// - Returns: ``TCDcError`` that holds the same error and context.
    public func toDcError() -> TCDcError {
        guard let code = TCDcError.Code(rawValue: self.error.rawValue) else {
            fatalError("Unexpected internal conversion error!\nPlease file a bug at https://github.com/teco-project/teco to help address the problem.")
        }
        return TCDcError(code, context: self.context)
    }
}

extension TCDcError.UnsupportedOperation {
    /// - Returns: ``TCCommonError`` that holds the same error and context.
    public func toCommonError() -> TCCommonError? {
        if let context = self.context, let error = TCCommonError(errorCode: self.error.rawValue, context: context) {
            return error
        }
        return nil
    }
}
