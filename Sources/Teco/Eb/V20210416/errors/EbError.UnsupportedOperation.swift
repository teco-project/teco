//===----------------------------------------------------------------------===//
//
// This source file is part of the Teco open source project.
//
// Copyright (c) 2022 the Teco project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
// See CONTRIBUTORS.txt for the list of Teco project authors
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by TecoServiceGenerator.
// DO NOT EDIT.

extension TCEbError {
    public struct UnsupportedOperation: TCErrorType {
        enum Code: String {
            case invalidAction = "UnsupportedOperation.InvalidAction"
            case invalidEndpointType = "UnsupportedOperation.InvalidEndpointType"
            case other = "UnsupportedOperation"
        }
        
        private let error: Code
        
        public let context: TCErrorContext?
        
        public var errorCode: String {
            self.error.rawValue
        }
        
        public init ?(errorCode: String, context: TCErrorContext) {
            guard let error = Code(rawValue: errorCode) else {
                return nil
            }
            self.error = error
            self.context = context
        }
        
        internal init (_ error: Code, context: TCErrorContext? = nil) {
            self.error = error
            self.context = context
        }
        
        /// 连接器操作失败，接口不支持当前操作。
        public static var invalidAction: UnsupportedOperation {
            UnsupportedOperation(.invalidAction)
        }
        
        /// 连接器创建失败，不支持当前后端服务类型。
        public static var invalidEndpointType: UnsupportedOperation {
            UnsupportedOperation(.invalidEndpointType)
        }
        
        /// 操作不支持。
        public static var other: UnsupportedOperation {
            UnsupportedOperation(.other)
        }
    }
}

extension TCEbError.UnsupportedOperation: Equatable {
    public static func == (lhs: TCEbError.UnsupportedOperation, rhs: TCEbError.UnsupportedOperation) -> Bool {
        lhs.error == rhs.error
    }
}

extension TCEbError.UnsupportedOperation: CustomStringConvertible {
    public var description: String {
        return "\(self.error.rawValue): \(message ?? "")"
    }
}

extension TCEbError.UnsupportedOperation {
    public func toEbError() -> TCEbError {
        guard let code = TCEbError.Code(rawValue: self.error.rawValue) else {
            fatalError("Unexpected internal conversion error!\nPlease file a bug at https://github.com/teco-project/teco to help address the problem.")
        }
        return TCEbError(code, context: self.context)
    }
}