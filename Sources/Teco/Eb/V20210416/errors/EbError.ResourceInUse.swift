//===----------------------------------------------------------------------===//
//
// This source file is part of the Teco open source project.
//
// Copyright (c) 2022 the Teco project authors
// Licensed under Apache License v2.0
//
// See LICENSE.txt for license information
//
// SPDX-License-Identifier: Apache-2.0
//
//===----------------------------------------------------------------------===//

// THIS FILE IS AUTOMATICALLY GENERATED by TecoServiceGenerator.
// DO NOT EDIT.

extension TCEbError {
    public struct ResourceInUse: TCErrorType {
        enum Code: String {
            case defaultEventBus = "ResourceInUse.DefaultEventBus"
            case eventBus = "ResourceInUse.EventBus"
            case rule = "ResourceInUse.Rule"
        }
        
        private let error: Code
        
        public let context: TCErrorContext?
        
        public var errorCode: String {
            self.error.rawValue
        }
        
        /// Initializer used by ``TCClient`` to match an error of this type.
        ///
        /// You should not use this initializer directly as there are no public initializers for ``TCErrorContext``.
        public init ?(errorCode: String, context: TCErrorContext) {
            guard let error = Code(rawValue: errorCode) else {
                return nil
            }
            self.error = error
            self.context = context
        }
        
        internal init (_ error: Code, context: TCErrorContext? = nil) {
            self.error = error
            self.context = context
        }
        
        /// 同一个地域下，云服务默认事件集不允许重复创建。
        public static var defaultEventBus: ResourceInUse {
            ResourceInUse(.defaultEventBus)
        }
        
        /// 事件集下有规则或者连接器，无法删除，请删除所有规则和连接器后重试。
        public static var eventBus: ResourceInUse {
            ResourceInUse(.eventBus)
        }
        
        /// 规则下有目标，无法删除，请删除所有目标后重试。
        public static var rule: ResourceInUse {
            ResourceInUse(.rule)
        }
    }
}

extension TCEbError.ResourceInUse: Equatable {
    public static func == (lhs: TCEbError.ResourceInUse, rhs: TCEbError.ResourceInUse) -> Bool {
        lhs.error == rhs.error
    }
}

extension TCEbError.ResourceInUse: CustomStringConvertible {
    public var description: String {
        return "\(self.error.rawValue): \(message ?? "")"
    }
}

extension TCEbError.ResourceInUse {
    /// - Returns: ``TCEbError`` that holds the same error and context.
    public func toEbError() -> TCEbError {
        guard let code = TCEbError.Code(rawValue: self.error.rawValue) else {
            fatalError("Unexpected internal conversion error!\nPlease file a bug at https://github.com/teco-project/teco to help address the problem.")
        }
        return TCEbError(code, context: self.context)
    }
}

extension TCEbError.ResourceInUse {
    /// - Returns: ``TCCommonError`` that holds the same error and context.
    public func toCommonError() -> TCCommonError? {
        if let context = self.context, let error = TCCommonError(errorCode: self.error.rawValue, context: context) {
            return error
        }
        return nil
    }
}
